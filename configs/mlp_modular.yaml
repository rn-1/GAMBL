# MLP on modular arithmetic — same task/hyperparams as transformer for comparison.
base_config: configs/base.yaml
model: mlp
prime: 97
operation: "+"
train_fraction: 0.5
embed_dim: 128
hidden_dim: 512
num_mlp_layers: 3
activation: relu
weight_decay: 1.0
n_steps: 100000
seed: 42
